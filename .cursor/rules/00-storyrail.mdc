---
description: StoryRail repository workflow rules
alwaysApply: true
---

<!-- Generated by StoryRail. Safe to regenerate. -->
# StoryRail Rules

- Keep changes scoped to the active StoryRail spec.
- Do not implement before `technical-design.md`, `test-plan.md`, and `tasks.yaml` exist.
- Run the checks listed in `storyrail.yaml` before calling work complete.
- Treat auth, payment, quota, secrets, migrations, and production config as high-risk areas.
