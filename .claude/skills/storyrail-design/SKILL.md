---
name: storyrail-design
description: Use StoryRail specs and tasks as the source of truth before implementation.
---

# StoryRail Design

Read `AGENTS.md`, `CLAUDE.md`, `.specify/memory/constitution.md`, and the active `specs/<run-id>/` directory before editing code.

Do not bypass auth, billing, quota, secrets, migrations, production config, or user data gates.
