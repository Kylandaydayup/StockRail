# Generated by StoryRail. Safe to regenerate.
name: StoryRail CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  storyrail-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: StoryRail placeholder check
        run: |
          test -f storyrail.yaml
          echo "StoryRail repo config detected."
