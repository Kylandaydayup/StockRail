# Generated by StoryRail. Safe to regenerate.
name: StoryRail Preview

on:
  pull_request:

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build preview
        run: |
          echo "Replace this with the repository preview deployment command."
      - name: Publish preview URL
        run: |
          echo "preview_url=file://preview" >> "$GITHUB_OUTPUT"
