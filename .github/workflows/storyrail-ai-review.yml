# Generated by StoryRail. Safe to regenerate.
name: StoryRail AI Review

on:
  pull_request:

jobs:
  pr-agent:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: PR-Agent review placeholder
        run: |
          echo "Integrate PR-Agent or Codex Review here."
          echo "StoryRail expects AI review before CI is considered ready."
